import React from 'react'

export default function App() {
  return (
    <div style={{padding: 24, fontFamily: 'Inter, system-ui, Arial'}}>
      <h1>yl-core</h1>
      <p>Minimal WYSIWYG low-code editor prototype (React + Vite + TypeScript + @dnd-kit).</p>
      <p>这是一个最小可运行的原型骨架，你可以根据需要扩展编辑器功能。</p>
    </div>
  )
}